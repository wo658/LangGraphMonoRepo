// TypeScript LangGraph code snippets
export const typescriptSnippets: Record<string, string> = {}

// Helper function to get a random snippet (deprecated)
export function getRandomTypeScriptSnippet(): string {
  return ''
}

// Helper function to detect if code is TypeScript (deprecated)
export function isTypeScriptCode(_code: string): boolean {
  return false
}